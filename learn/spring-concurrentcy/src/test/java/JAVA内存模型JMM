一般这样定义：
   逻辑模型
  线程 --> 工作内存 ——————>

  线程 --> 工作内存 ——————> Save/Load --> 主存

  线程 --> 工作内存 ——————>

  工作内存 一般对应CPU的寄存器和高速缓存
  主存对于计算机主存

稍后需要明确的：
   工作内存对应什么？
   主存又对应什么？
   堆和栈是什么情况？什么样子？


内存的8种操作：
   lock(锁定)：作用于主存的变量，把变量标识为独占
   unlock(解锁)：作用于主存，释放独占
   read(读取)：作用于主存，讲主存变量传输到工作内存中
   load(载入)： 作用于工作内存，把传输获得的数据，放入工作内存中
   use(使用)：作用不工作内存，把数据赋给执行引擎
   assign(赋值)：作用于工作内存，把执行引擎的值给工作内存
   store(存储)：作用于主存，把工作内存的值传回主存
   write(写入)： 将传回的数据，写入主存中