CPU多级缓存：在CPU和主存之间是存在多级缓存的，因为CPU的处理速度远远大于主存的数据，可以理解为内存和磁盘。所有包含着CPU cache 归属于CPU
当然所有就有了缓存一致性的问题：MESI协议 本地读写，远程读写
M：被修改的 被CPU修改，在未来的某个时间段内，需要写回主存 与主存数据不一致
E：独享的 缓存在CPU cache中未被修改的 当其他CPU读取时候，变为共享的
S：共享的 被多个CPU 缓存， 如果有一个CPU修改了，那么就要通知广播作废，变更好无效状态
I：无效的 无效的状态，只有这种状态是不允许读的，需要重新从主存中读取

时间局部性：一个数据如果被访问了，那么大概率这个数据在未来的某个时间段也会被访问
空间局部性：一个数据如果被访问了，那么这个数据的相邻的数据也大概率在未来的某个时间段内被访问。

CPU多级缓存： 优化，乱序执行优化